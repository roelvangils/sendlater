{
  "name": "sendlater",
  "version": "0.1.0",
  "private": true,
  "description": "Schedule Gmail sends via labels on drafts (workaround for Mimestream).",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "push": "clasp push",
    "open": "clasp open",
    "logs": "clasp logs --watch",
    "docs:build": "node build-docs.mjs"
  },
  "devDependencies": {
    "@google/clasp": "^2.4.2",
    "@types/google-apps-script": "^1.0.83",
    "markdown-it": "^14.1.1",
    "typescript": "^5.4.0"
  }
}
